## Git commits

Unless the task or project instructions specify a commit style, inspect the
repository's existing history (`git log --oneline --reverse`) and match its
prevailing message format — subject line structure, capitalization,
scope/category prefixes, and body conventions — for new commits.
